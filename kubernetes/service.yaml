apiVersion: v1
kind: Service
metadata:
  name: ai-voice-service

spec:
  selector:
    app: ai-voice-assistant

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: LoadBalancer
