# AI-Voice-Support-Assistant
This project is a real-time AI Voice Support Assistant that accepts live audio input, converts speech to text using Whisper, performs semantic retrieval using RAG, generates AI responses using an LLM, streams responses back in near real-time, uses Redis queues for async processing, uses Docker + Kubernetes deployment setup.
